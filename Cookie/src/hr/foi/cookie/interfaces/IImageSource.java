package hr.foi.cookie.interfaces;

import android.graphics.Bitmap;

/**
 * Specifies that the type of data that can be uniquely grabbed is
 * a Bitmap type.
 * 
 * @author Marin
 */
public interface IImageSource extends IUniquelyGettable<Bitmap> {
	
}
